stage: "stage2"
mode: "inference"
model: "tbd"

masking:
  cloud_threshold: 60.0
  compose_mode: "replace_cloud_only"

io:
  observed_channels: [0, 1, 2, 3] # observed S2 (RGB+NIR)
  sar_channels: [4, 5]           # VV, VH
  fmask_channel: 6

infer:
  subset:
    mode: "index_top_n"
    n_patches: 64
    seed: 0

